# FND-005 local-only guard. Copy to .env.local or provide the same values
# through the current process environment. Never commit effective values.
TRAZACTIVO_LOCAL_ENV=development
TRAZACTIVO_LOCAL_CONFIRMATION=FND-005
TRAZACTIVO_SQL_PORT=14333

# Required secrets. Values must remain empty in this tracked example.
MSSQL_SA_PASSWORD=
TRAZACTIVO_PLATFORM_DB_PASSWORD=
TRAZACTIVO_CLIENT_A_DB_PASSWORD=
TRAZACTIVO_CLIENT_B_DB_PASSWORD=
